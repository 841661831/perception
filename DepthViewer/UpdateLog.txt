已修复，没替换    http协议中检测来源更改为0和1 ，没有其他值了
已经更改，但没替换， 在ipCheckProcess中添加检查磁盘大小的线程，当磁盘使用超过90%，则删除savedata中最早的1个文件夹，每30s触发一次，当savedata中文件夹数量不足3个时，不再删除

当前版本：v2.11_20210609
修复bug：
修改:
1、更改车辆类型的协议，与单基站同步
2、 算法更新：（1）航向角和车道号查询，（2）跟踪的尺寸保持
3、软件更新：（1）将全域id记录在配置文件中，每次重启软件会读取配置文件，id累加记录，而不是重新从0开始记录
优化：增加全景选项，可以俯视整个隧道

当前版本：v2.10_20210607
修复bug：
修改:事件修改内容
优化：增加全景选项，可以俯视整个隧道

当前版本：v2.9_20210602
修复bug：
修改:修改事件
优化：根据配置文件中雷达的数量在界面上生成相应数量的下拉框，可以将视角切换到对应的每个基站位置

当前版本：v2.7_20210522
修复bug：
修复使用在线模式时，pcpyglwidget中的drawcubeinfo会抛出异常的bug，增加了判断在线还是离线的判断解决此bug
优化
修改:
1、保存resultData加入新的保存字段
2、org_data按照每1小时保存一个文件
3、增加日志记录信息
4、事件数据与E1帧数据分开保存，在配置文件中设置是否保存标志位 bSaveE1  bSaveEvent

当前版本：v2.6_20210514
修复bug：
1、修复http发送时丢包的bug
2、修复http发送的类别错误bug
3、修复http发送的航向角错误
优化：
修改:

当前版本：v2.5_20210513
修复bug：
1、修复udp传输数据车道线为0的bug
2、统一udp传输的全域发送时间戳为在commonDefine中定义的getPcBoxInfo函数中获取的时间
优化：
修改:
1、发送给平台数据方式由udp替换为http，增加新的协议字段，详细信息见隧道接口文档

当前版本：v2.4_20210508
修复bug：
优化：
1、增加新功能：双击右键回到原点坐标，在界面丢失时可以回到原点
2、优化保存org_data方式，每次启动软件以时间戳创建文件夹，將数据保存进去，不需要每次启动时耗费大量时间删除原始org_data数据
修改:

当前版本：v2.3_20210422
修复bug：
优化：
修改:
1、添加离线平台给的JSON文件读取功能
2、import second修改为import second

当前版本：v2.2_20210421
修复bug：
1、修复离线暂停后，历史轨迹持续更新的BUG
2、修复根据目标类型过滤导致目标显示不完全的BUG
优化：
修改:

当前版本：v2.1_20210420
修复bug：
优化：
修改:
1.更新代码目录结构,上一个目录结构打包报错

当前版本：v2.0_20210419
修复bug：
优化：
修改:
1.更新代码目录结构,删除冗余的算法文件
