首要优化项：
0、授权的漏洞：mac地址可被修改
1、视频目标序号不固定
2、在线模式没有点云数据时，切换到离线模式，页显示不了点云。原因是嗅探器阻塞，导致无法从在线数据读取进程中退出。  111
3、新的内外参
5、相机中途断开链接，点云播放的等待时间较长，而且等相机重新连接后，也无法读取到视频 222
6、若本机IP与相机IP不在统一网段，则创建vid时会非长慢
7、算法都挪到项目中后，打包运行失败，numba的错误
8、在线切换到离线后，刷新变慢
12、融合不稳定，视频目标框闪烁
1、优化逻辑，考虑加锁
4、算法耗时太久，尝试把无关的放到另外但进程中
5、表格排序
10、缩小视频保存的文件大小
11、每次启动很耗时


次要优化项：
10、接口程序，希望远程查看各个站点的设备状况！
20、软件打包出售方案，调研一下方案


已解决
1、点云和视频分别用了自己但usStatus，导致从自动切换到手动时，点云但usStatus未变为0！目前，暂行方案：pcAlgOnline中设置1<=usStatus<=2时，暂停算法
2、8号门的相机，通道1有问题，比实际延时1秒左右，通过与通道4比较即可看出，所以，之前测的视频一直比点云慢1秒左右，就是这个问题导致的！
3、转发数据不对多原因是，配置文件删掉了IP188的一行，导致原始数据也向41发送，所以抓包的数据是点云原始数据
4、融合结果转发，导致崩溃，center[0]超范围，是循环listBoxInfo.append()的问题，导致相机与相机对比融合，以至于超范围！
5、添加IP判断
6、转发原始数据的port和识别结果的port一样？？不一样！
7、融合之后的目标框序号，在点云图上找不到对应的！ 原因是匹配失败的boxid是自己添加的，所以找不到
8、点云模型加密,删除无用文件
9、授权激活方式修改，把永久激活改为永久激活和重置试用期两种激活方式，由激活码而定
10、激活码还需再简化，验证，还有，时间计数好像不太对

sudo ifconfig enp5s0 192.168.3.183 netmask 255.255.255.0


关于python全局变量修改：
（1）把dict比作鱼缸，里面的k,v比作缸里装的鱼。现在只是把鱼缸丢给了函数，这个鱼缸本身你不能改，但是里面的鱼可以。
（2）当包含全局变量的模块被重复载入时，之前对全局变量多修改就无效啦！



